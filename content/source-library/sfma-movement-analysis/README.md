# SFMA Movement Analysis

Place the earlier anatomy document and all of its LaTeX sources in `original/` without
modifying them. Use `working-notes/` to assess which concepts or methods remain useful.

Treat this as a historical conceptual source rather than the default organizing
framework for the redesigned Movement Systems course.